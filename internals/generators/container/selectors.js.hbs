/**
*  Components are imported in specific (scope based) order:
*  1. Node_modules
*  2. Application
*  3. Module
*/
import { createSelector } from 'reselect';


/**
* {{ camelCase name }}
* @desc TODO: description for {{ camelCase name }}
*/
const select{{ properCase name }}Domain = () => state => state.get('{{ camelCase name }}');


/**
* {{ camelCase name }}
* @desc TODO: description for {{ camelCase name }}
*/
const select{{ properCase name }} = () => createSelector(
  select{{ properCase name }}Domain(),
    ( substate ) => {
    return {}; //This is where you need to add the state for the container
    }
);

export default select{{ properCase name }};
export {
  select{{ properCase name }}Domain,
};
